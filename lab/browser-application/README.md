# Browser carrier laboratory fixture

These JavaScript sources preserve the historical anonymous browser carrier,
streaming decoder and lifecycle tests. They are source-only test fixtures.

They are not loaded by `naivefox_transport`, not copied into the production
application template, and not included in the released template archive.
Production `assets/app.js` is ordinary operator content served verbatim.
